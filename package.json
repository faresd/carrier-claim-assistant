{
  "name": "carrier-claim-assistant",
  "version": "2.11.1",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js tests/*.test.mjs && npm --prefix monitor-service test",
    "generate:assets": "node scripts/generate-icons.mjs && node scripts/generate-store-assets.mjs",
    "verify:release": "node scripts/verify-release.mjs",
    "package": "bash scripts/package-extension.sh"
  }
}
